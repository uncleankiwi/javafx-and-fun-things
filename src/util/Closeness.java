package util;


/*

 */
/**
 * Holds a get() method that determines the closeness of two numbers.
 */
public final class Closeness {

	/**
	 * Basically -log(|reference - value|) * k, but using a string implementation.
	 *
	 * <p></p>
	 *
	 * @return Closeness of two numbers.
	 */
	public static int get() {
		return 0;
	}
}
